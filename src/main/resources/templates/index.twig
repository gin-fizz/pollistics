{% extends layouts/main.twig %}

{% set pageTitle = "Pollistics | Home" %}

{% block content %}
<p>{{ greeting }}</p>
<ul>
	{% for poll in polls %}
    <li>
        <a href="/polls/{{ poll.id }}">{{ poll.name }} {{ poll.options }}</a>
    </li>
    {% endfor %}
</ul>
{% endblock %}